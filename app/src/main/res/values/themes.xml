<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MarvelComics" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brandColor</item>
        <item name="colorPrimaryVariant">@color/brandColor</item>
        <item name="colorOnPrimary">@color/brandColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brandColor</item>
        <item name="colorSecondaryVariant">@color/brandColor</item>
        <item name="colorOnSecondary">@color/brandColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>